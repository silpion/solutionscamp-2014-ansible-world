---
- hosts: localhost
  connection: local
  gather_facts: false
  vars:
    - topics:
      - "Infrastructure automation vs project automation"
      - "Ansible"
      - "Ansible - Test Driven"
      - "Ansible - Project automation"
  tasks:
    - name: Describe this talk
      with_items: topics
      debug: msg="{{ item }}"
